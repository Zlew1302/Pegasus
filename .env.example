# Pflicht: Anthropic API Key fuer Agent-Ausfuehrung
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Brave Search API Key fuer Web-Recherche
# Ohne Key nutzen Agents ihr internes Wissen als Fallback
BRAVE_API_KEY=

# Datenbank (Standard: SQLite im Backend-Ordner)
DATABASE_URL=sqlite+aiosqlite:///./crewboard.db

# Frontend: Backend-URL (nur aendern wenn Backend nicht auf localhost:8000 laeuft)
NEXT_PUBLIC_API_URL=http://localhost:8000

# CORS: Erlaubte Origins (kommasepariert fuer mehrere)
CORS_ORIGINS=["http://localhost:3000"]
